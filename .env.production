# Sportzy Frontend - Production Environment Configuration
# Copy this file to .env.production and update with actual production values
# This file is used when NODE_ENV=production

# ============================================================================
# Production API Configuration
# ============================================================================

# Backend API Base URL - Use HTTPS in production
VITE_API_URL=https://api.sportzy.com

# WebSocket URL for real-time updates - Use WSS (WebSocket Secure) in production
VITE_WS_URL=wss://api.sportzy.com

# ============================================================================
# Optional: Advanced Configuration
# ============================================================================

# Application environment
NODE_ENV=production

# Host binding for Docker containers
HOST=0.0.0.0

# Server port (usually controlled by Docker port mapping)
PORT=3000

# ============================================================================
# Notes
# ============================================================================
# - Always use HTTPS (not HTTP) in production
# - Always use WSS (not WS) for WebSocket connections in production
# - Keep sensitive credentials in CI/CD secrets, not in .env files
# - This file should NOT be committed to version control
# - For Docker, pass environment variables via docker-compose.yml or docker run -e flags
